import { ApiVersion } from '../loader/abstract-component-dto';
/**
 * 比较版本号的方法,版本号的格式是以[0-9]+\.的形式组成，例如1.0.0.0
 * 如果字符串不是以上面的规则组成，则直接进行字符串比较
 * @param first
 * @param second
 * @return number 返回结果大于0表示first比second大，
 * 等于0表示相等，小于0表示first比second小
 */
export declare function compareVersion(first: string, second: string): number;
/**
* 判断 metaVersion 是否在版本区间内
* @param metaVersion 待检测版本号
* @param metaVersionList 版本号区间（升序排列）
* @returns
* -1: 小于最小值
*  0: 在区间内
*  1: 大于最大值
*/
export declare function isInMetaVersionList(metaVersion: string, metaVersionList: readonly string[]): number;
/**
 * 比较apiVersion
 *
 * @param apiVersion1
 * @param apiVersion2
 * @return boolean 返回表示两个apiVersion是否相等
 */
export declare function isEqualApiVersion(apiVersion1: ApiVersion | undefined | null, apiVersion2: ApiVersion | undefined | null): boolean;
/**
 * 判断apiVersion是否有效
 *
 * @param apiVersion
 * @return boolean 返回是否有效apiVersion
 */
export declare function isValidApiVersion(apiVersion: ApiVersion | undefined | null): boolean;
/**
 * 判断apiVersion是否有效
 *
 * @param apiVersionStr
 * @return boolean 返回是否有效apiVersion
 */
export declare function isValidApiVersionStr(apiVersionStr: string | undefined | null): boolean;
/**
 * 比较apiVersion
 *
 * @param apiVersion1
 * @param apiVersion2
 * @return number 返回结果大于0表示first比second大，
 * 等于0表示相等，小于0表示first比second小
 */
export declare function compareApiVersion(apiVersion1: ApiVersion | undefined | null, apiVersion2: ApiVersion | undefined | null): number;
/**
 * 判断值是否为null、undefined、''
 * @param value
 */
export declare function isEmpty(value: string | undefined | null): boolean;
/**
 * 判断值是否为null、undefined、''
 * @param value
 */
export declare function isNotEmpty(value: string | undefined | null): boolean;
